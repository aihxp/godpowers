apiVersion: godpowers/v1
kind: Recipe
metadata:
  name: add-feature-tiny
  category: feature-addition
  description: "Tiny scope feature: typo, config, 1-line change"

triggers:
  intent-keywords:
    - "add feature tiny"
    - "typo"
    - "config tweak"
    - "one line"
    - "trivial change"
    - "tiny fix"

sequences:
  default:
    description: "Tiny scope feature: typo, config, 1-line change"
    steps:
      - command: "/god-fast"
        why: "No planning ceremony; direct edit + tests pass"

default-sequence: default
