apiVersion: godpowers/v1
kind: Recipe
metadata:
  name: whats-next
  category: meta
  description: "Want to know what to run next"

triggers:
  intent-keywords:
    - "whats next"
    - "what now"
    - "next step"
    - "next command"

sequences:
  default:
    description: "Want to know what to run next"
    steps:
      - command: "/god-next"
        why: "Reads disk state, suggests with reason"

default-sequence: default
