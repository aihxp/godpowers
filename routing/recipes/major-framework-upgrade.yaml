apiVersion: godpowers/v1
kind: Recipe
metadata:
  name: major-framework-upgrade
  category: maintaining
  description: "Major framework version upgrade"

triggers:
  intent-keywords:
    - "react 19"
    - "node 22"
    - "framework upgrade"
    - "major version"
    - "migrate"

sequences:
  default:
    description: "Major framework version upgrade"
    steps:
      - command: "/god-upgrade"
        why: "Expand-contract pattern with metric-gated slices"

default-sequence: default
