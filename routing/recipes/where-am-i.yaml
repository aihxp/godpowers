apiVersion: godpowers/v1
kind: Recipe
metadata:
  name: where-am-i
  category: meta
  description: "See current project state"

triggers:
  intent-keywords:
    - "where am i"
    - "current state"
    - "project status"

sequences:
  default:
    description: "See current project state"
    steps:
      - command: "/god-status"
        why: "Re-derive state from disk"

default-sequence: default
