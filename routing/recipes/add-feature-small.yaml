apiVersion: godpowers/v1
kind: Recipe
metadata:
  name: add-feature-small
  category: feature-addition
  description: "Small feature: 1-3 hours, with TDD discipline"

triggers:
  intent-keywords:
    - "add feature small"
    - "add new feature"
    - "small feature"
    - "small task"
    - "few hours"
    - "with TDD"

sequences:
  default:
    description: "Small feature: 1-3 hours, with TDD discipline"
    steps:
      - command: "/god-quick"
        why: "TDD-disciplined small task; one atomic commit"

default-sequence: default
