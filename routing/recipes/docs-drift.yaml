apiVersion: godpowers/v1
kind: Recipe
metadata:
  name: docs-drift
  category: maintaining
  description: "Documentation has drifted from code"

triggers:
  intent-keywords:
    - "docs drift"
    - "fix readme"
    - "documentation outdated"

sequences:
  default:
    description: "Documentation has drifted from code"
    steps:
      - command: "/god-docs"
        why: "Verifies every claim against code"

default-sequence: default
