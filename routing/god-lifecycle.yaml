apiVersion: godpowers/v1
kind: CommandRouting
metadata:
  command: /god-lifecycle
  description: Show project phase
  tier: 0

prerequisites:
  required:
    []

execution:
  spawns: [built-in]
  context: fresh
  writes:
    []


success-path:
  next-recommended: varies

failure-path:
  on-error: /god-doctor

endoff:
  state-update: tier-0 updated for /god-lifecycle
  events: [agent.start, artifact.created, agent.end]
