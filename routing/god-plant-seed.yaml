apiVersion: godpowers/v1
kind: CommandRouting
metadata:
  command: /god-plant-seed
  description: Forward-looking idea with trigger condition
  tier: 0

prerequisites:
  required: []

execution:
  spawns: [built-in]
  context: fresh
  writes: []

success-path:
  next-recommended: /god-next

failure-path:
  on-error: /god-doctor

endoff:
  state-update: tier-0 updated for /god-plant-seed
  events: [agent.start, agent.end]
