apiVersion: godpowers/v1
kind: CommandRouting
metadata:
  command: /god-graph
  description: Build/query project knowledge graph
  tier: 0
  family: capture

prerequisites:
  required: []

execution:
  spawns: [built-in]
  context: fresh
  writes: []

success-path:
  next-recommended: varies
  outcome:
    type: contextual
    label: Context-specific next route
    reason: The next route depends on current disk state, command arguments, or user choice.
    allowed-next: [/god-status, /god-next, /god-help]

failure-path:
  on-error: /god-doctor

endoff:
  state-update: tier-0 updated for /god-graph
  events: [agent.start, agent.end]
